# TODO

## Bug Triage

- Runtime has bugs.
- Exercise 4: The TypeScript code does not show the message in the comment. Improve the `lerInput` text to: "Use await lerInput("...") replacing the text with the questions you want to ask the user, to ask them to write something."
- Exercise 8: The non-completion message expects the value 10, but should accept 6 or more.
- Exercise 17: The code example has a validation error.
- Exercise 20: The number 8 goes off-screen in certain cases.
- Exercise 22: In the hint, explain that a string is a list of characters and suggest using `.length` to validate the word length.
- Exercise 23: Correct the text "SPbY" in the text area.

## Feature Triage

- Add explanatory animations instead of running text, for greater student engagement.
- Create an interactive tutorial for the student.
- Add a button to cancel exercise execution.
- Add visual editing elements (e.g., letter color in the 1st chapter, font size in the 2nd, letter color in the 3rd, etc.).
- Exercise 11: In the hints, remind that fruits must be strings and explain the meaning of "of" in the `for...of` loop. The term `for` must be in code format.
- Exercise 13: Add information about the functions to turn green and red on/off.
- Exercise 14 and 15: Swap the order of these exercises.
- Exercise 16: In the hints, show how to use the "!==" (not equal) operator.
- Exercise 17: Explain the use of `await` earlier, as it is used before this point.
- Add an end session button, showing time, points, a motivational message, and encouraging the student to share on social networks.
- Exercise 18: Indicate the names of the functions to move sideways and backwards.
- Exercises 19, 20 and 21: Make execution faster.
- Exercise 13 onwards: There is no need to add `const podeAvancar: boolean = true;` as it is already predefined in the code.

## Task Status

| Task name [feature]/[bug] | To implement | Implementing | Ready | Responsible |
|---------------------------|--------------|--------------|-------|--------------|
| Example task lorem ipsum  | ✔️           |              |       | To assign    |

When a task leaves triage and goes to the table, it must be marked with [feature] or [bug] in the name.
